import React from 'react';

const Topbar = () => {
  return (
    <header style={{ background: '#fff', padding: '20px', borderBottom: '1px solid #ddd' }}>
      <h2>Topbar</h2>
    </header>
  );
};

export default Topbar;
